# Environment Variables Template
# Copy to .env.local or .env.development and modify as needed

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================

# Set the minimum log level for the application logger
# Options: debug, info, warn, error
# Default: info (development), error (production)
VITE_LOG_LEVEL=info

# Enable or disable all console output (including direct console.log calls)
# Options: true, false
# Default: true (development), false (production recommended)
VITE_ENABLE_CONSOLE_LOGS=true

# ============================================================================
# DEVELOPMENT TOOLS
# ============================================================================

# Enable the debug panel for development (only shows in dev mode)
# The debug panel provides app state inspection, environment vars, and performance metrics
# Options: true, false
# Default: false
VITE_ENABLE_DEBUG_PANEL=true

# ============================================================================
# API CONFIGURATION (if needed)
# ============================================================================

# Uncomment and set these if you need to override default API settings
# VITE_API_BASE_URL=http://localhost:8080
# VITE_API_TIMEOUT=10000

# ============================================================================
# NOTES
# ============================================================================
# - All VITE_ variables are available in the client-side code
# - Variables without VITE_ prefix are only available during build
# - Use .env.local for local development overrides (git ignored)
# - Use .env.development for shared development settings
# - Use .env.production for production builds